get_filename_component(st_app_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
include(CMakeFindDependencyMacro)

if(NOT TARGET ST::app)
  include("${st_app_CMAKE_DIR}/st_appTargets.cmake")
endif()

set(st_app_LIBRARIES ST::app)
